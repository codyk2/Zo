<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>$(DEVELOPMENT_LANGUAGE)</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>$(PRODUCT_BUNDLE_PACKAGE_TYPE)</string>
	<key>CFBundleShortVersionString</key>
	<string>1.0</string>
	<key>CFBundleVersion</key>
	<string>1</string>
	<key>LSRequiresIPhoneOS</key>
	<true/>
	<key>NSMicrophoneUsageDescription</key>
	<string>EMPIRE uses the microphone so you can push-to-talk. Everything is processed on this device by Gemma 4 on Cactus — no audio leaves the phone.</string>
	<!-- NSCameraUsageDescription — required for the "+ Film product" flow
	     (SellerCaptureShim) that lets the seller film a 10s product clip
	     and upload it to the Mac for intake. Gated behind FeatureFlags.sellerMode. -->
	<key>NSCameraUsageDescription</key>
	<string>EMPIRE uses the camera so you can film a short product clip. The video uploads to your Mac running EMPIRE for the AI to describe and sell.</string>
	<!-- Backend endpoint for all iPhone → Mac calls (classify, sell-video,
	     WS dashboard, state, etc.). Accepts EITHER:
	       - a LAN hostname/IP     e.g. 172.20.10.2  → http://host:8000
	       - a full URL            e.g. https://xxx.trycloudflare.com → used as-is
	     Long-press the REC pill or the failure banner in the iPhone app to
	     override at runtime (writes to UserDefaults, wins over this value).
	     Find LAN IP via:  ipconfig getifaddr en0
	     Spawn a tunnel via: cloudflared tunnel --url http://localhost:8000
	     Leave empty to disable backend calls entirely. -->
	<key>EmpireBackendHost</key>
	<string>https://piano-superintendent-coverage-ease.trycloudflare.com</string>
	<!-- Allow plain HTTP to LAN (the Mac backend serves http://, not https://).
	     Local-network only by default — no exception for arbitrary domains. -->
	<key>NSAppTransportSecurity</key>
	<dict>
		<key>NSAllowsLocalNetworking</key>
		<true/>
	</dict>
	<!-- Bonjour service is needed for iOS 14+ local-network permission prompt
	     even when we use a static IP. The system asks the user to allow local
	     network access on first request. -->
	<key>NSLocalNetworkUsageDescription</key>
	<string>EMPIRE talks to the Mac running the EMPIRE backend over your local network so Gemma 4 can classify comments. No data leaves your network.</string>
	<key>UIApplicationSceneManifest</key>
	<dict>
		<key>UIApplicationSupportsMultipleScenes</key>
		<false/>
	</dict>
	<key>UILaunchScreen</key>
	<dict>
		<key>UIColorName</key>
		<string></string>
		<key>UIImageName</key>
		<string></string>
	</dict>
	<key>UIRequiredDeviceCapabilities</key>
	<array>
		<string>armv7</string>
	</array>
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
	</array>
	<key>UISupportedInterfaceOrientations~ipad</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationPortraitUpsideDown</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
</dict>
</plist>
