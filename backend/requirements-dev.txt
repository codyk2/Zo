# Test + lint deps. NOT included in production deploy — install via:
#   pip install -r requirements-dev.txt
# in the same venv as requirements.txt.
-r requirements.txt

pytest>=8.0
pytest-asyncio>=0.23
respx>=0.21
ruff>=0.4
