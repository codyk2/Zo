node_modules/
venv/
__pycache__/
*.pyc
.env
.DS_Store
dist/
build/
*.egg-info/
.expo/
.agents/

# Local lipsync benchmark renders / scratch outputs (large mp4s, not source-of-truth)
phase0/bench/videos/
phase0/renders_e2e/

# Generated media (carousel spins, pitch renders, comment renders, voice answers)
# Cody's local_answers regenerate via scripts/render_local_answers.py.
# Per-request outputs in backend/renders/ are derived from substrates + audio.
backend/renders/
backend/local_answers/
# Per-request audio-first dispatches (TTS mp3s for the dashboard to play
# immediately while Wav2Lip catches up). Regenerated on every comment +
# every pitch (pitches reuse this same dir with a `pitch_dispatch_` prefix).
backend/response_audio/
*.log
